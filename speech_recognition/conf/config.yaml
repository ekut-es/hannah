defaults:
    - dataset: kws
    - compress: null
    - trainer: default
    - module: default
    - checkpoint: default
    - backend: null
    - early_stopping: null
    - profiler: null

type: train
experiment_id: test
output_dir: trained_models
auto_lr: false
fold_bn: -1
seed: 1234

hydra:
    run:
      dir: ${output_dir}/${experiment_id}/${model.name}/
    sweep:
      dir: ${output_dir}/${experiment_id}/${model.name}/

#TODO:
dump_test: false
input_file: ''
