# @package _global_
defaults:
  - override /dataset: cifar10
  - override /module: image_classifier
  - override /features: null
  - override /model: timm_efficientnet_lite1

experiment_id: mytest_vision
module:
  batch_size: 64
