defaults:
    - base_config
    - override dataset: kws
    - override features: mfcc
    - override model: ofa
    - override scheduler: null
    - override optimizer: adamw
    - override normalizer: fixedpoint
    - override module: stream_classifier
    - override trainer: default
    - override nas: ofa_nas
    - _self_

experiment_id: progressive_shrinking
module:
    shuffle_all_dataloaders: True

optimizer:
    lr: 0.0001

dataset:
  data_folder: ${hydra:runtime.cwd}/../../datasets/
