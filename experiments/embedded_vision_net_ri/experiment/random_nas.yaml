# @package _global_
defaults:
  - override /nas: random_nas
  - override /model: embedded_vision_net


nas:
  budget: 300
  n_jobs: 8


trainer:
  max_epochs: 10

seed: [1234]

oxperiment_id: "random_nas"
