# @package _global_

defaults:
  - override /model: timm_resnet18_mc_dropout

experiment_id: sweep_drop_percent1
trainer:
  max_epochs: 50

hydra:
  sweeper:
    params:
      dataset.drop_labels: 0.9,0.8,0.7,0.6,0.5,0.3
  sweep:
    subdir: ${model.name}/drop_labels=${dataset.drop_labels}
