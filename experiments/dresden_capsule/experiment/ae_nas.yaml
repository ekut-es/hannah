# @package _global_
defaults:
  - override /nas: aging_evolution_nas
  - override /model: lazy_convnet


nas:
  budget: 200
  n_jobs: 8


trainer:
  max_epochs: 5


model:
  labels: 4

seed: [1234]

experiment_id: "ae_nas"
