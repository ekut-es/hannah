# @package _global_
defaults:
  - override /nas: random_nas
  - override /model: embedded_vision_nas


nas:
  budget: 100
  n_jobs: 8
  presample: False


trainer:
  max_epochs: 10

seed: [1234]

experiment_id: "random_nas_log_saving"
