# @package _global_
defaults:
  - override /nas: random_nas
  - override /model: embedded_vision_net
  - override /dataset: cifar10

model:
  num_classes: 10
nas:
  budget: 600
  n_jobs: 8

seed: [1234]

experiment_id: "random_nas_cifar10_filter_weights_batch"
