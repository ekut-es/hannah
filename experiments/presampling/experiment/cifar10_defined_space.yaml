# @package _global_
defaults:
  - override /nas: defined_space_exploration
  - override /model: embedded_vision_net
  - override /dataset: cifar10

model:
  num_classes: 10
nas:
  budget: 600
  n_jobs: 8

seed: [1234]

experiment_id: "defined_space_exploration"
