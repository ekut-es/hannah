

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  POETRY_HOME: "$CI_PROJECT_DIR/.poetry"
  GIT_SUBMODULE_STRATEGY: recursive

#cache:
#  paths:
#-  ./venv

before_script:
  - cat /etc/os-release
  - source /etc/os-release
  - if [[ $ID == "debian" || $ID == "ubuntu" ]]; then
  -   apt update
  -   apt -y install python3-dev freeglut3-dev portaudio19-dev libblas-dev liblapack-dev
  - else
  -   yum install epel-release -y
  -   yum install git sudo python36 -y
  -   yum install wget curl -y
  -   yum groupinstall 'Development Tools' -y
  - fi
  - python3 -m ensurepip
  - python3 -V # Print out python version for debugging
  - python3 -m pip install virtualenv
  
  # Install poetry using recommended method
  - curl -sSL https://raw.githubusercontent.com/python-poetry/poetry/master/get-poetry.py | python3
  - export PATH=${POETRY_HOME}/bin:${PATH}
  

  # create virtual environment for tests 
  - virtualenv venv -p python3
  - source venv/bin/activate
 

  # install and manage dependencies with poetry
  - poetry install
  - cd datasets
  - ./get_datasets.sh
  - cd ..
  

test:
  image: centos:7
  script:
    - set -e 
    - source venv/bin/activate
    - python -m pytest test
    - python -m speech_recognition.train --seed 1234 --model tc-res8  --experiment-id ci  --n-epoch=1
    - python -m speech_recognition.train --seed 1234 --model tc-res8 --experiment-id ci_prof --n-epoch=1 --profile
    - python -m speech_recognition.train  --model tc-res8 --compress distillation/quant_aware_train_fp/quant_aware_train_fixpoint_quant_4_4.yaml --n-epoch=3 --fold-bn 1  --normalize-bits 8     
    - python -m speech_recognition.train --dump-test --experiment-id branchy_search-2exits_8_6 --num-workers 24 --normalize-bits 8 --fold-bn 1 --model branchy-tc-res8 --gpu-no 1  --compress distillation/quant_aware_train_fp/quant_aware_train_fixpoint_quant_8_6.yaml --earlyexit_thresholds 1.0 1.0  --earlyexit_lossweights 0.3 0.3 --n-epoch 3 --lr 0.01
  tags:
    - docker



test_python_37:
  image: python:3.7
  script:
    - set -e 
    - source venv/bin/activate
    - python -m pytest test
    - python -m speech_recognition.train --seed 1234 --model tc-res8  --experiment-id ci  --n-epoch=5 --no-cuda
    - python -m speech_recognition.train --seed 1234 --model tc-res8 --experiment-id ci_prof --n-epoch=1 --profile
    - python -m speech_recognition.train  --model tc-res8 --compress distillation/quant_aware_train_fp/quant_aware_train_fixpoint_quant_4_4.yaml --n-epoch=3 --fold-bn 1  --normalize-bits 8 
  tags:
    - docker
